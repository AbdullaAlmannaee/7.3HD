{
  "scripts": {
    "start": "node server.js",
    "test": "jest --ci --reporters=default --reporters=jest-junit",
    "lint": "eslint .",
    "sonar": "sonar-scanner"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "jest": "^29.0.0",
    "jest-junit": "^16.0.0",
    "sonarqube-scanner": "^2.8.1"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
