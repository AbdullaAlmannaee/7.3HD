services:
  app:
    build: .
    image: ${IMAGE_NAME:-sit223-demo}:${IMAGE_TAG:-latest}
    ports:
      - "8088:3000"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3000"]
      interval: 30s
      timeout: 3s
      retries: 3
